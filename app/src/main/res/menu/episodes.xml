<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        custom:showAsAction="collapseActionView|always"
        custom:actionViewClass="androidx.appcompat.widget.SearchView"
        android:title="@string/search_label"/>

    <item
        android:id="@+id/refresh_item"
        android:title="@string/refresh_label"
        android:menuCategory="container"
        custom:showAsAction="always"
        android:icon="@drawable/ic_refresh"/>

    <item
        android:id="@+id/add_podcast_item"
        android:title="@string/add_feed_label"
        android:menuCategory="container"
        custom:showAsAction="always"
        android:visible="false"
        android:icon="@drawable/ic_add"/>

    <item
        android:id="@+id/filter_items"
        android:icon="@drawable/ic_filter"
        android:menuCategory="container"
        android:title="@string/filter"
        android:visible="false"
        custom:showAsAction="ifRoom"/>

    <item
        android:id="@+id/mark_all_item"
        android:title="@string/mark_all_label"
        android:menuCategory="container"
        custom:showAsAction="collapseActionView"
        android:visible="false"
        android:icon="@drawable/ic_check"/>

    <item
        android:id="@+id/mark_all_read_item"
        android:title="@string/mark_all_read_label"
        android:menuCategory="container"
        custom:showAsAction="collapseActionView"
        android:visible="false"
        android:icon="@drawable/ic_check"/>

    <item
        android:id="@+id/remove_all_new_flags_item"
        android:title="@string/remove_all_new_flags_label"
        android:menuCategory="container"
        custom:showAsAction="collapseActionView"
        android:visible="false"
        android:icon="@drawable/ic_check"/>

    <item
        android:id="@+id/paused_first_item"
        android:title="@string/paused_first_label"
        app:actionViewClass="android.widget.CheckBox"
        android:checkable="true"
        android:menuCategory="container"
        custom:showAsAction="collapseActionView"
        android:visible="false"
        android:checked="true" />
    <item
        android:id="@+id/inbox_mode_item"
        android:title="@string/inbox_mode_label"
        app:actionViewClass="android.widget.CheckBox"
        android:checkable="true"
        android:menuCategory="container"
        custom:showAsAction="collapseActionView"
        android:visible="false"
        android:checked="true" />

</menu>
