<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:squareImageView="http://schemas.android.com/apk/de.danoeh.antennapod"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:id="@+id/cover_fragment"
    android:padding="8dp"
    android:gravity="center">

    <de.danoeh.antennapod.ui.common.SquareImageView
        android:id="@+id/imgvCover"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="8dp"
        android:layout_weight="0"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:importantForAccessibility="no"
        android:scaleType="fitCenter"
        squareImageView:direction="height"
        tools:src="@android:drawable/sym_def_app_icon" />

    <LinearLayout
        android:id="@+id/cover_fragment_text_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/txtvPodcastTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="none"
            android:gravity="center_horizontal"
            android:maxLines="2"
            android:textSize="@dimen/text_size_small"
            android:textColor="?android:attr/textColorSecondary"
            android:textIsSelectable="true"
            tools:text="Podcast" />

        <TextView
            android:id="@+id/txtvEpisodeTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="none"
            android:gravity="center_horizontal"
            android:textSize="@dimen/text_size_small"
            android:maxLines="2"
            android:textColor="?android:attr/textColorPrimary"
            android:textIsSelectable="true"
            tools:text="Episode" />

        <RelativeLayout
            android:id="@+id/chapter_control"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp">

            <ImageButton
                android:id="@+id/butPrevChapter"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_alignParentStart="true"
                android:layout_alignParentLeft="true"
                android:layout_centerHorizontal="true"
                android:layout_centerVertical="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/prev_chapter"
                android:scaleType="fitCenter"
                app:srcCompat="?attr/av_chapter_prev"
                tools:srcCompat="@drawable/ic_av_chapter_prev_white_24dp" />

            <TextView
                android:id="@+id/txtvChapterTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_toStartOf="@+id/butNextChapter"
                android:layout_toLeftOf="@+id/butNextChapter"
                android:layout_toEndOf="@+id/butPrevChapter"
                android:layout_toRightOf="@+id/butPrevChapter"
                android:gravity="center"
                android:ellipsize="end"
                android:maxLines="1"
                android:singleLine="true"
                android:textColor="?android:attr/textColorPrimary"
                android:textIsSelectable="false"
                android:textSize="@dimen/text_size_small"
                tools:text="Chapter" />

            <ImageButton
                android:id="@+id/butNextChapter"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_alignParentEnd="true"
                android:layout_alignParentRight="true"
                android:layout_centerHorizontal="true"
                android:layout_centerVertical="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/next_chapter"
                android:scaleType="fitCenter"
                app:srcCompat="?attr/av_chapter_next"
                tools:srcCompat="@drawable/ic_av_chapter_next_white_24dp" />


        </RelativeLayout>

    </LinearLayout>

</LinearLayout>
